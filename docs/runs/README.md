# docs/runs — saved evidence

Saved model outputs kept as evidence that things work (referenced in design reviews).
Example: outputs from `practice_examples/week01/hello_llm.py`, or a pipeline `results.json` copy.
This folder is tracked; large generated artifacts (`results.json`, `*.db`, `logs/`) are gitignored.
